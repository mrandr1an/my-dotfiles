{
  "layer": "top",
  "modules-left": ["sway/workspaces", "battery", "clock"],
  "modules-center": [],
  "modules-right": ["network", "pulseaudio","custom/volume"],

  "clock": {
    "format": "{:%Y-%m-%d %H:%M:%S}"
  },

  "battery": {
    "format": "{capacity}%"
  },

  "network": {
    "interface": "wlan0"  // Change to your network interface
  },

  "pulseaudio": {
    "format": "{volume}%"
  },

  "sway/workspaces": {
    "format": "{name}"
  },

  "custom/volume": {
  "exec": "~/.config/waybar/scripts/audio.sh",
  "interval": 1,
  "on-click": "wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle",
  "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+",
  "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-"
 }
}


